#include<stdio.h>
int main()
{
    float n, sum=0;

    printf("Input the number of terms : ");
    scanf("%f", &n);

    for(float i=1; i<=n; i++) { printf("1/%.0f + ", i); sum+= 1/i; }

    printf("\nSum of Series upto %.0f terms : %f", n, sum);

    return 0;
}
