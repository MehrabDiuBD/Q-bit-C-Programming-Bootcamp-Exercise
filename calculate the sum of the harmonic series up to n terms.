#include<stdio.h>
int main()
{
    int n;
    double total = 0;
    printf("Input the number of terms : ");
    scanf("%d", &n);
    for(double i=1; i<=n; i++) { total+= 1/i; }
    printf("Sum of Series upto %d terms : %lf", n, total);
    return 0;
}
