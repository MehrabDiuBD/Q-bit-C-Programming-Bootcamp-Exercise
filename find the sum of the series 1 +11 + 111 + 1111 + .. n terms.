#include<stdio.h>

int main() {
    int n;
    printf("Input the number of terms: ");
    scanf("%d", &n);

    for(int i = 1; i <= n; i++) {
        printf("%d ", (int)pow(10, i) + 1);
    }

    return 0;
}
